import React from "react";
import TabbedGrid from "../components/topics.tsx";

const Main = (props) => {
  return (
    <div className="main">
      <TabbedGrid />
    </div>
  );
};

export default Main;
